export enum Routes {
    Login = "/login",
    Forum = "/forum",
    Registration = "/registration",
    Profile = "/profile",
    LeaderBoard = "/leaders",
    Start = "/start",
    Rules = "/rules",
    ApplicationError = "/application-error",
    ServerError = "/server-error",
    Game = "/game"
}
